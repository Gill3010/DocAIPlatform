# Copiar a .env si quieres que el frontend use otro backend (otro host/puerto o producción).
# Si no existe .env, la app usa el mismo host que la página con puerto 8000.

# URL base del backend (sin /api/v1)
VITE_API_URL=http://localhost:8000
