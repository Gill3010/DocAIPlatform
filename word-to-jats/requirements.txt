# Word-to-JATS Conversion Platform - Dependencies
# Core dependencies for Lambdas and local execution

# XML processing (JATS manipulation)
lxml>=5.3.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# HTTP client (GROBID API)
httpx>=0.27.0
requests>=2.31.0

# AWS CDK (infrastructure)
aws-cdk-lib>=2.170.0
constructs>=10.4.0

# Document processing (preprocessor, fallback)
python-docx>=1.1.0

# Pandoc bridge (subprocess)
# pandoc must be installed at system level

# Logging
structlog>=24.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
